# Ya Korea Guesthouse Kiosk Project — Claude Instructions

## 작업 컴퓨터 식별 규칙

이 프로젝트는 두 컴퓨터에서 동시에 작업한다. 매 응답 **첫 줄**에 반드시 아래 헤더를 표기한다.

| 작업 디렉토리 | 헤더 |
|---|---|
| `C:\project\...` | `[키오스크 PC]` |
| `C:\Project_Claude\...` | `[노트북]` |

**이유:** 두 컴퓨터가 같은 git 레포를 공유하지만 역할이 다름. Claude가 잘못된 컴퓨터 기준으로 안내하면 사용자에게 혼란을 초래함.

### 구분 기준
- 반드시 **절대 경로**로 식별 (`C:\project` vs `C:\Project_Claude`)
- 두 컴퓨터 모두 `chat-system/` 폴더가 있으므로 폴더 존재 여부로 판단하지 말 것

### 배포 흐름
- **채팅 위젯 수정** (chat-system/): 키오스크 PC 또는 노트북에서 수정 → `git push` → 상대 컴퓨터에서 `git pull` → **노트북에서 Railway 배포**
- **키오스크 페이지 수정** (pages/, js/, css/, data/): 키오스크 PC에서 수정 → `git push` → 노트북에서 `git pull` (필요 시)

## 대원칙 — 3개의 독립된 자아로 작업

모든 작업(문제 파악·구현·디버깅)은 **3개의 독립된 자아**로 수행한다. 이는 가장 중요한 대원칙이다.

| 자아 | 역할 |
|------|------|
| **자아 1 — 계획** | 문제를 분석하고 해결 계획을 세운다 |
| **자아 2 — 실행** | 자아 1의 계획을 그대로 실행한다 |
| **자아 3 — 평가** | 자아 2의 실행 결과를 비판적으로 평가하고 자아 1에게 조언한다 |

**이유:** 계획·실행·평가를 하나의 자아로 통합하면 스스로 "잘했다"고 평가하고 넘어가는 편향이 생긴다. 세 자아를 독립적으로 분리해야 객관적 검증이 가능하다.

응답 시 각 단계를 `[자아 1 — 계획]`, `[자아 2 — 실행]`, `[자아 3 — 평가]` 헤더로 명시한다.

## Git 브랜치 규칙

- **사용 브랜치: `master` 단독 사용**
- `main` 브랜치는 2026-05-07에 삭제됨 → 태그 `archive/main-2026-05-07`로 보관
- 양쪽 PC 모두 `master` 브랜치만 사용할 것
- push/pull 시 항상 `master` 기준으로 동작하는지 확인
