// =============================================================
// config.js  ─  키오스크 전체 설정값 모음
//
// ✅ 이 파일만 수정하면 됩니다:
//   - 체크인 가능 시간 변경  → CHECK_IN_HOUR
//   - 예약 데이터 파일 경로  → EXCEL_URL
//   - 엑셀 컬럼 구조 변경   → EXCEL_COLUMNS
//   - 계단/통로 안내 대상 방 → BACK_STAIR_ROOMS / FRONT_PASS_ROOMS
// =============================================================

const CONFIG = {

  // ----------------------------------------------------------
  // 1. 체크인 시간 규칙
  //    CHECK_IN_HOUR: 체크인 가능 시작 시각 (24h, 이 시각 이후에만 검색 허용)
  //    CHECK_IN_END_HOUR: 체크인 마감 시각 (이 시각 이후는 프론트 닫힘)
  // ----------------------------------------------------------
  CHECK_IN_HOUR: 15,   // 15:00 이후 체크인 가능
  CHECK_IN_END_HOUR: 23,   // 23:00 이후 프론트 닫힘

  // ----------------------------------------------------------
  // 2. 예약 데이터 (엑셀)
  //    EXCEL_URL: reservations.xlsx 경로 (reservation_number.js 기준 상대경로)
  //    EXCEL_COLUMNS: 엑셀의 컬럼 순서가 바뀌면 여기만 수정
  // ----------------------------------------------------------
  EXCEL_URL: "../data/reservations.xlsx",

  EXCEL_COLUMNS: {
    guestName: 0,   // A열: 손님 이름
    reservationNumber: 7,   // H열: 예약 번호
    roomNumber: 20,  // U열: 방 번호
    checkOutDate: 24   // Y열: 체크아웃 날짜
  },

  // ----------------------------------------------------------
  // 3. 비밀번호 데이터 파일 경로
  // ----------------------------------------------------------
  PASSWORDS_URL: "../data/passwords.json",

  // ----------------------------------------------------------
  // 4. 동선 안내 대상 방 목록
  //    BACK_STAIR_ROOMS  : 카운터 뒤쪽 계단을 이용해야 하는 방
  //    FRONT_PASS_ROOMS  : 카운터와 주방 사이를 통과해야 하는 방
  //    (방번호 문자열로 관리 — "205-1" 같은 침대 방은 앞 번호만 비교)
  // ----------------------------------------------------------
  BACK_STAIR_ROOMS: new Set([
    "201", "202", "203", "204", "205", "206", "207", "208",
    "301", "302", "303", "304", "305"
  ]),

  FRONT_PASS_ROOMS: new Set([
    "211", "212",
    "306", "307", "308", "309",
    "401", "402", "403",
    "B1"
  ]),

  // ----------------------------------------------------------
  // 5. 자동 새로고침 / 비활동 타이머 (app.js 에서 사용)
  //    INACTIVITY_TIMEOUT_MS : 손님 무조작 시 메인으로 복귀 (ms)
  //    PERIODIC_RELOAD_MS    : 주기적 메인 복귀 간격 (ms)
  //    DAILY_RELOAD_AT       : 매일 강제 새로고침 시각 목록
  // ----------------------------------------------------------
  INACTIVITY_TIMEOUT_MS: 60_000,       // 1분
  PERIODIC_RELOAD_MS: 10 * 60_000,  // 10분
  DAILY_RELOAD_AT: ["14:59"],    // 체크인 1분 전 데이터 갱신

  // ----------------------------------------------------------
  // 6. 움직임 감지 (index.html 에서 사용)
  // ----------------------------------------------------------
  MOTION: {
    COOLDOWN_MS: 90_000,   // 음성 재생 후 쿨다운 (1분 30초)
    REQUIRED_ACTIVE_MS: 3_000,   // 움직임 지속 시간 임계값
    DIFF_THRESHOLD: 9_000_000,  // 픽셀 차이 임계값
    FLASH_INTERVAL_MS: 200,   // 테두리 깜빡임 간격
    FLASH_BORDER_PX: 100,   // 테두리 두께 (px)
    VOICE_SETS: 2,   // EN→ZH 세트 반복 횟수
    VOICE_FILE_COUNT: 9,   // EN_1~9, ZH_1~9
    ACTIVE_HOURS: { start: 7, end: 23 }  // 음성 안내 활성 시간대
  }

};

// 다른 스크립트에서 window.CONFIG 로 접근 가능
window.CONFIG = CONFIG;
